spring:
  datasource:
    url: jdbc:h2:mem:dbi-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  data:
    mongodb:
      database: buecherei_test
  mongodb:
    embedded:
      version: 4.9.0

app:
  runner:
    enabled: false
  mongo:
    index:
      auto-create: false
      field: title

